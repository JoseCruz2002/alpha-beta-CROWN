# Configuration file for running the safeNLP benchmark (all properties).
general:
  complete_verifier: mip
  device: cpu
  normal_run: false
  enable_incomplete_verification: true
model:
  onnx_path: complete_verifier/models/vnncomp24/safenlp/medical/perturbations_0.onnx
specification:
  type: box
  norm: .inf
  epsilon: null
attack:
  pgd_order: after
  pgd_restarts: 10000
